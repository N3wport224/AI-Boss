name: analyze_metrics
tier: workflow
entrypoint: workflows.example_workflow:MetricsAnalysisWorkflow
description: Derives a churn-risk signal from the raw metrics produced upstream.
enabled: true
inputs:
  - name: risk_threshold
    label: Risk threshold
    type: number
    default: 0.1
