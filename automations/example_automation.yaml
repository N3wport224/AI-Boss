name: fetch_raw_metrics
tier: automation
entrypoint: automations.example_automation:DataFetchAutomation
description: Simulates pulling raw metrics from an external system on a fixed schedule.
enabled: true
